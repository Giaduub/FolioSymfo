{% extends 'base.html.twig' %}

{% block body %}
<article>
     <h2>{{ project.Title }}</h2>
     <div class="metadata">Ecrit le 10/05/2021 à 15:00 dans la catégorie Projet</div>
     <div class="content">
         <img src="{{ project.Image }}" alt="">
         {{ project.Description | raw}}
         <a href="{{ path('projets')}}" class="btn btn-primary">Retour</a>
     </div>
 </article>
{% endblock %}