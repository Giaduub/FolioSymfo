{% extends 'base.html.twig' %}

{% block body %}


{% if not app.user %}
<article>
     <h2>{{ project.Title }}</h2>
     <div class="metadata">Ecrit le 10/05/2021 à 15:00 dans la catégorie Projet</div>
     <div class="content">
         <img src="{{ project.Image }}" alt="">
         {{ project.Description | raw}}
         <a href="{{ path('projets')}}" class="btn btn-primary">Retour</a>
         {% else %}
         <article>
     <h2>{{ project.Title }}</h2>
     <div class="metadata">Ecrit le 10/05/2021 à 15:00 dans la catégorie Projet</div>
     <div class="content">
         <img src="{{ project.Image }}" alt="">
         {{ project.Description | raw}}
         <a href="{{ path('projets')}}" class="btn btn-primary">Retour</a>
         <a href="{{ path('projet_edit', {'id': project.id})}}" class="btn btn-primary">Modifier</a>
         <a href="{{path('delete', {'id' : project.id})}}"><button class="btn btn-danger">Delete</button></a>
     </div>
 </article>

 {% endif %}
{% endblock %}